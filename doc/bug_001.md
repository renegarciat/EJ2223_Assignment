`rotorSizer.solve();          % run until convergence`
            % Eq. (9)  outer bridge tangential length
            h_ob = (tau_r - 2*w_hr - b_ps) / 2;
            if h_ob <= 0
                error('IPMRotorSizer:geometry', ...
                    'h_ob <= 0 (%.3f mm). Reduce AlphaM or Whr_fraction.', h_ob);
            end
h_ob = -5.251 mm
Solution: Not a bug, but the error should be more explicit, saying that the geometry is imposible. Improve the code by making it check the geometry of AlphaM and Whr_fraction make sense.